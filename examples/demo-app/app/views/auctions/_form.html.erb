<%= form_with(model: auction, local: true) do |form| %>
  <% if auction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auction.errors.count, "error") %> prohibited this auction from being saved:</h2>

      <ul>
      <% auction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.text_field :item_id, id: :auction_item_id %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount, id: :auction_amount %>
  </div>

  <div class="field">
    <%= form.label :bidder %>
    <%= form.text_field :bidder, id: :auction_bidder %>
  </div>

  <div class="field">
    <%= form.label :bid %>
    <%= form.text_field :bid, id: :auction_bid %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
